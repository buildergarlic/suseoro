[project]
name = "suseoro-v2"
version = "2.2.1"
description = "Simple local book acquisition for school librarians"
requires-python = ">=3.11"
dependencies = [
    "argon2-cffi>=23.1,<26",
    "defusedxml>=0.7,<1",
    "fastapi>=0.115,<0.120",
    "openpyxl>=3.1,<4",
    "pillow>=10,<13",
    "pypdf>=6,<7",
    "pdfplumber>=0.11,<0.12",
    "pypdfium2>=4.30,<6",
    "python-multipart>=0.0.20,<1",
    "python-calamine>=0.5,<0.7",
    "rapidfuzz>=3.10,<4",
    "tzdata>=2025.2",
    "uvicorn>=0.30,<1.0",
]

[project.optional-dependencies]
desktop = ["pywebview>=5,<7", "pyinstaller>=6.16,<7"]

[project.scripts]
suseoro = "suseoro.simple.desktop:main"

[dependency-groups]
dev = [
    "httpx>=0.27,<1.0",
    "odfpy>=1.4,<2",
    "pytest>=8.3,<9.0",
    "xlwt>=1.3,<2",
]

[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff.lint]
# FastAPI intentionally represents dependency injection with call-valued defaults.
ignore = ["B008"]

[tool.hatch.build.targets.wheel]
packages = ["src/suseoro"]

[tool.hatch.build]
include = ["src/suseoro/**/*.py", "src/suseoro/db/migrations/*.sql"]
